# Security

Do not submit API keys, session cookies, billing screenshots containing personal information, or private vendor-account data.

The local verification agent only needs public pricing/docs URLs and a local Ollama endpoint. If you later add GitHub PR automation, use a fine-grained token with the minimum repository permissions required and keep it in environment variables, never in YAML or committed `.env` files.
